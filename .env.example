# Environment variables for local report generator application

# Gemini API Key (optional - can also be configured in the UI)
GEMINI_API_KEY=your_gemini_api_key_here

# Flask application port
FLASK_PORT=5000
